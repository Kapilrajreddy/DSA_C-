#include <bits/stdc++.h>
using namespace std;

void addTwoNumbers(int a, int b) {
    int sum = a + b;
    cout << sum << endl;
}

int main() {
    int a, b;
    cin >> a >> b;
    addTwoNumbers(a, b);
    return 0;
}